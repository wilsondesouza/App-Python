Leia-me até o final

- GamePass.exe (versão codeline e com interface gráfica) -
Para que o programa funcione sem maiores problemas e seja executado até o final, além das observações feitas
dentro do próprio programa, onde é pedido a instalação do emulador "Bluestacks" e do game "Fallout Shelter",
é necessário executá-lo sem outros programas, pastas ou navegadores abertos, onde seus ícones ocupam espaço 
na barra de tarefas. Isto é necessário, pois parte do código do programa utiliza da biblioteca PyAutoGui, 
onde se faz necessário a captura do clique do mouse em posições específicas da tela.

- PesquisaBing.exe (versão codeline e com interface gráfica) -
Sem maiores etapas e observações para executar além das apontadas dentro do próprio programa. 
O programa utiliza do selenium para automatizar os comandos
(por favor, deixe o Edge como navegador padrão antes e enquanto o programa é executado).

Read-me untilthe end

- GamePass.exe (Codeline version and GUI version)
For the program to run smoothly and run until the end, in addition to the observations made
within the program itself, where the installation of the "Bluestacks" emulator and the game "Fallout Shelter" 
is requested, it is necessary to run it without other programs, folders or browsers open, where its icons take up space
spaces on the taskbar. This is necessary, because part of the program code uses the PyAutoGui library, where 
it is necessary to capture mouse clicks in specific positions on the screen.

- PesquisaBing.exe (Codeline version and GUI version)
No further steps and observations to perform other than those pointed out within the program itself.
The program uses selenium to automate the commands
(please put Edge as default browser before and while running the program).
